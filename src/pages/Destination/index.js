import React from 'react';
import {Text, View} from 'react-native';

const Destination = () => {
  return (
    <View>
      <Text>Destination Page</Text>
    </View>
  );
};

export default Destination;

// const styles = StyleSheet.create({});
